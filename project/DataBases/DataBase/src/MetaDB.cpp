#include "MetaDB.h"

void MetaDB::addNewFileInfo(const FileInfo &file) {
        return;
}

void MetaDB::changeFileInfo(FileInfo &file) {
        return;
}

std::vector<FileInfo> MetaDB::getUserFileInfo(const UserDate &user) {
        std::vector<FileInfo> fileInfo;
        return fileInfo;
}
