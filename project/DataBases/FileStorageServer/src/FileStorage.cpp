#include "FileStorage.h"

void connect() {
        return;
}
void disconnect() {
        return;
}
void addChunk(const std::vector<Chunk> &chunks) {
        return;
}
std::vector<Chunk> getChunk(const std::vector<Chunk> &chunks) {
        std::vector<Chunk> chunk;
        return chunk;
}
