#pragma once
/*#include <vector>
#include<memory>
#include "LocalDB.h"

struct Chunks {
        int id;
        int idFile;
};

class ChunkDB {
public:
        void getChunks(const Chunks &chunks);
        void addChunks(const Chunks &chunks);
private:
        Chunks chunk;
        int fileId;
        std::shared_ptr<LocalDB> localDB;
};*/
